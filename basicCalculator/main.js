function calculator(num1, operator, num2) {
  if (num2 === 0) {
    Return "Can't divide by 0!";
  } else {
    switch (operator) {
	Case “+”:
    Return Num1 + num2;
    Case: “-”:
    Return num1 - num2;
    Case: “*”:
    Return num1 * num2;
    Case: “/”
    Return num1 / 2;
  }
}
}
